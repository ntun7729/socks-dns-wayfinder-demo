## Question

How should the client and server be configured and supervised on Ubuntu when the user requires root-capable deployment but the implementation itself should minimize privilege?

The answer must define configuration and secret-file locations, bind-address defaults, systemd unit behavior, restart and shutdown semantics, logging, resource limits, and hardening options. It must clearly state which parts require root and which can run as an unprivileged service account.
